{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "https://raw.githubusercontent.com/nf-core/mcmicro/master/assets/schema_input.json",
    "title": "nf-core/mcmicro pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": ".*$",
                "errorMessage": "Sample name must be provided and cannot contain spaces"
            },
            "cycle_number": {
                "type": "string",
                "pattern": ".*$",
                "errorMessage": "Need to provide a path to image files directory."
            },
            "channel_count": {
                "type": "integer",
                "errorMessage": "Need to provide the number of channels"
            },
            "image_tiles": {
                "type": "string",
                "pattern": ".*$",
                "errorMessage": "Need to provide a file containing image tiles"
            }
        },
        "required": ["sample","cycle_number","channel_count","image_tiles"]
    }
}
